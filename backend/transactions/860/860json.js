function transformToStructuredJSON860(data) {
  // Transform the data to structured JSON format for 860 transactions
  // This is a placeholder function; actual implementation will depend on the data structure
  return {
    transactionType: '860',
    details: data
  };
}

module.exports = {
  transformToStructuredJSON860
};
